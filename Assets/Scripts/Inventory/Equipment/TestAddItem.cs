/*
using UnityEngine;

public class TestAddItem : MonoBehaviour
{
    public Inventory playerInventory; // Drag the Inventory component of the player here
    public Equipment testEquipment; // Drag the Equipment you created in the Editor here

    private void Update()
    {
        // For demonstration purposes: Add item to inventory when pressing the 'A' key
        if (Input.GetKeyDown(KeyCode.A))
        {
            playerInventory.Add(testEquipment);
            Debug.Log(testEquipment.itemName + " added to inventory!");
        }
    }
}
*/