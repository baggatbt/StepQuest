public enum CrafterEntityType
{
    None = 0,

    // Generators
    WoodGenerator = 1,
    OreGenerator = 2,

    // Wood chain
    Wood = 10,
    Plank = 11,
    Handle = 12,

    // Ore chain
    Ore = 20,
    Ingot = 21,
    Blade = 22,

    // Crafted result
    IronSword = 30,

    // Intrusion
    RogueEnemy = 100
}