public enum CrafterEntityType
{
    None = 0,

    // Special placed objects
    Chest = 1,

    // Generators
    WoodGenerator = 10,
    OreGenerator = 11,

    // Wood chain
    Wood = 20,
    Plank = 21,
    Handle = 22,

    // Ore chain
    Ore = 30,
    Ingot = 31,
    Blade = 32,

    // Crafted result
    IronSword = 40,

    // Intrusion
    RogueEnemy = 100
}